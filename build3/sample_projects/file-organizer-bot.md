# File Organizer Bot

Started this to clean up my Downloads folder automatically. Sorts files into
subfolders by extension (images, docs, installers, etc). Handles duplicate
filenames by appending a number instead of overwriting.

Status: working version done, runs on a schedule. Want to add a "dry run"
mode so I can preview changes before it actually moves anything.
